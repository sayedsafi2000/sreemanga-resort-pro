# Coolify environment variables — reference list (the full guide is DEPLOY_COOLIFY.md).
# docker-compose.yml derives everything it can; you only fill the email provider (and Stripe if used).

# Generated by Coolify automatically (do not type these):
#   SERVICE_USER_POSTGRES, SERVICE_PASSWORD_POSTGRES   database credentials
#   SERVICE_PASSWORD_64_JWT                            JWT signing secret
#   SERVICE_URL_SERVER / SERVICE_URL_ADMIN / SERVICE_URL_WEB   from the domains you assign

# Email — set ONE transport (required for guest OTP / booking emails):
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# RESEND_API_KEY=
# BREVO_API_KEY=
# BREVO_EMAIL=
EMAIL_FROM=Pina Vista <noreply@pinavista.com>
RESORT_NAME=Pina Vista

# Optional
# STRIPE_SECRET_KEY=            # card payments; webhook POST https://api.<domain>/api/public/stripe/webhook
# STRIPE_WEBHOOK_SECRET=
# CORS_ORIGIN=                  # default: admin URL,web URL
# API_RATE_LIMIT_PER_MIN=600    # per signed-in user
# AUTH_RATE_LIMIT_PER_15MIN=20  # login attempts per IP
# JWT_EXPIRES_IN=7d
# POSTGRES_DB=resort_management
# SEED_ON_EMPTY_DB=true         # seed demo data on the very first boot only
# RUN_SEED=false                # set true for one deploy to force the seed
